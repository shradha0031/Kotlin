fun main() {
    val emptyList = emptyList<String>()
    val emptySet = emptySet<Int>()
    val emptyMap = emptyMap<String, Int>()


//    | Function          | Returns            | Mutable?    |
//    | ----------------- | ------------------ | ----------- |
//    | `emptyList()`     | `List<T>`          | ❌ Immutable |
//    | `mutableListOf()` | `MutableList<T>`   | ✅ Mutable   |
//    | `emptySet()`      | `Set<T>`           | ❌ Immutable |
//    | `mutableSetOf()`  | `MutableSet<T>`    | ✅ Mutable   |
//    | `emptyMap()`      | `Map<K, V>`        | ❌ Immutable |
//    | `mutableMapOf()`  | `MutableMap<K, V>` | ✅ Mutable   |

}